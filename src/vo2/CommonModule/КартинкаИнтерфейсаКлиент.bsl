
&НаКлиенте
Функция КартинкаИнтерфейсаКлиент(Форма, ИмяКартинки)

	Если Форма = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	ИмяКартинкиИнтерфейса = СтрЗаменить(ИмяКартинки, "ESD", "Saby");
	
	Возврат Форма.Элементы[ИмяКартинкиИнтерфейса].Картинка;
	
КонецФункции

