
&НаКлиенте
Функция МодульКодаКлиент(ИмяМодуля)
	
	МодульОсновнойКонфигурации = ИмяМодуля = "ФайловаяСистемаКлиент";
	
	Если МодульОсновнойКонфигурации Тогда
		Возврат Вычислить("ФайловаяСистемаКлиент");
	Иначе
		Возврат ЭтаФорма;
	КонецЕсли;
	
КонецФункции

