
&НаСервере
Процедура УстановитьТипыОтветственногоЛица()
	
	// Физические лица
	МассивПроверяемыхТипов = Новый Массив;
	МассивПроверяемыхТипов.Добавить("Справочники.ФизическиеЛица");
		
	МодульКода("ESD_ТНОбщегоНазначенияСервер").УстановитьОграниченияТипов(Элементы.ФизЛицо, МассивПроверяемыхТипов);
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьИмяИНИИзНастроекСопоставления(ОтветственноеЛицо)
	
	Возврат;
	
КонецПроцедуры

