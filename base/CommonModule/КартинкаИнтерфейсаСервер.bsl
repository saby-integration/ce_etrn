
Функция КартинкаИнтерфейсаСервер(Форма, ИмяКартинки) Экспорт
	
	Если Форма = Неопределено Тогда
		Картинка = Неопределено;
	Иначе
		
		Если ТипЗнч(Форма) = Тип("ФормаКлиентскогоПриложения") И Форма.ЭтоВО Тогда			
			Картинка = Форма.Элементы[ИмяКартинки].Картинка;			
		Иначе 				
			Картинка = БиблиотекаКартинок[ИмяКартинки];			
		КонецЕсли;	
		
	КонецЕсли;	
		
	Возврат Картинка;
	
КонецФункции

