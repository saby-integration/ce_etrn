
#Область include_etrn_base_CommonModule_ТНОбщегоНазначенияКлиент_ИмяФормыОбработки
#КонецОбласти // include_etrn_base_CommonModule_ТНОбщегоНазначенияКлиент_ИмяФормыОбработки

&НаКлиенте
Функция ПараметрыОткрытияФормыОбработки(ИмяФормыДляОткрытия, Параметры = Неопределено, Владелец = Неопределено) Экспорт
	
	РезультатФункции = Новый Структура;
	РезультатФункции.Вставить("ИмяФормы",                    ИмяФормыОбработки(ИмяФормыДляОткрытия));
	РезультатФункции.Вставить("Параметры",                   Параметры);
	РезультатФункции.Вставить("Владелец",                    Владелец);
	РезультатФункции.Вставить("Уникальность",                Неопределено);
	РезультатФункции.Вставить("Окно",                        Неопределено);
	РезультатФункции.Вставить("НавигационнаяСсылка",         Неопределено);
	РезультатФункции.Вставить("ОписаниеОповещенияОЗакрытии", Неопределено);
	РезультатФункции.Вставить("РежимОткрытияОкна",           Неопределено);
	
	Возврат РезультатФункции;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуОбработки(ПараметрыОткрытияФормыОбработки) Экспорт
		
	ОткрытьФорму(
		ПараметрыОткрытияФормыОбработки.ИмяФормы,
		ПараметрыОткрытияФормыОбработки.Параметры,
		ПараметрыОткрытияФормыОбработки.Владелец,
		ПараметрыОткрытияФормыОбработки.Уникальность,
		ПараметрыОткрытияФормыОбработки.Окно,
		ПараметрыОткрытияФормыОбработки.НавигационнаяСсылка,
		ПараметрыОткрытияФормыОбработки.ОписаниеОповещенияОЗакрытии,
		ПараметрыОткрытияФормыОбработки.РежимОткрытияОкна);
	
КонецПроцедуры

&НаКлиенте
Функция ПолучитьФормуНаКлиенте(ИмяФормыНаКлиенте) Экспорт
	
	РезультатФункции = "ВнешняяОбработка.СБИС.Форма.";
	
	// BSLLS:GetFormMethod-off
	Возврат ПолучитьФорму(РезультатФункции + ИмяФормыНаКлиенте);
	// BSLLS:GetFormMethod-on
	
КонецФункции

