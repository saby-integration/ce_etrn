
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;	
				
	// ToDo: переделать в подписку на событие когда они заработают в разборщике 	
	РаботаСФайлами.ВыполнитьДействияПередЗаписьюПрисоединенногоФайла(ЭтотОбъект, Отказ);
	
КонецПроцедуры
