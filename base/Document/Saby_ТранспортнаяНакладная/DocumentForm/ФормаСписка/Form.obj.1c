  
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ТипПриИзменении(Элемент)
		
	УстановитьЭлементОтбораДинамическогоСписка(Список, Тип, "ТипЭтрН"); 
	
КонецПроцедуры

#КонецОбласти
    
#Область СлужебныеПроцедурыИФункции

&НаКлиентеНаСервереБезКонтекста
Процедура УстановитьЭлементОтбораДинамическогоСписка(Список, РеквизитФормы, ИмяПоляДинамическогоСписка)
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
		Список, 
		ИмяПоляДинамическогоСписка, 
		РеквизитФормы, 
		ВидСравненияКомпоновкиДанных.Равно,
		, 
		ЗначениеЗаполнено(РеквизитФормы));
	
КонецПроцедуры

&НаКлиенте
Процедура ОбменДаннымиСБИС(ИмяКоманды)	

    // ToDo: по доп командам нужна проверка по каждому документу
	// по возможности или не возможности применения выбранной команды 
	// - допустим возможна ли переадресация...
		 
	Если ИмяКоманды = "ЗагрузитьВСбис" Тогда 
		
		// Все выделенные строки
		
		// Авторизация
		context_params = Saby_ТНОбщегоНазначенияКлиент.АвторизацияSaby(ИмяКоманды, ЭтаФорма); 
		Если context_params <> Неопределено Тогда
			
			ВыделенныеСтроки = МассивСсылокВыделенныхСтрок();
			Для Каждого СсылкаДокумент Из ВыделенныеСтроки Цикл 
				//ДанныеНовогоДокумента = ПолучитьДанныеПодстановкиИСоздатьДокумент(Объект.Ссылка, context_params);
			КонецЦикла;
					
		КонецЕсли;
			
	ИначеЕсли ИмяКоманды = "ОткрытьВСбис" Тогда  		
		
		// 1 строка
		
		//ToDo: переделать на ЭтрН 
		СсылкаНаДокумент = ПолучитьСсылкуТест();
		
		Массив = Новый Массив;
		//Массив.Добавить(Объект.Ссылка);
		Массив.Добавить(СсылкаНаДокумент);
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Источник", Массив);
		ПараметрыФормы.Вставить("Форма",    ЭтаФорма);
		
		Saby_КомандыОбменаДляФормыКлиент.ПриНажатииОткрытьВСБИСПолучитьUID(ИмяКоманды, ПараметрыФормы);
		
	ИначеЕсли ИмяКоманды = "ПрочитатьДокумент" Тогда 	
		
		// запускает ини...
		
		//ToDo: переделать на ЭтрН 
		////////СсылкаНаДокумент = ПолучитьСсылкуТест();
		////////
		////////Массив = Новый Массив;
		//////////Массив.Добавить(Объект.Ссылка);
		////////Массив.Добавить(СсылкаНаДокумент);
		////////
		////////ПараметрыФормы = Новый Структура;
		////////ПараметрыФормы.Вставить("Источник",        Массив);
		////////ПараметрыФормы.Вставить("Форма",           ЭтаФорма);
		////////ПараметрыФормы.Вставить("ЭтоФормаОбъекта", Истина);
		////////
		////////Saby_КомандыОбменаДляФормыКлиент.ПриНажатииВыгрузитьВложенияИзСБИС(ИмяКоманды, ПараметрыФормы);

		// Авторизация
		context_params = Saby_ТНОбщегоНазначенияКлиент.АвторизацияSaby(ИмяКоманды, ЭтаФорма); 
		Если context_params <> Неопределено Тогда   
			//ДанныеДокументаСбис = ПолучитьДанныеДокументаСбис(Объект.Ссылка, context_params);
		КонецЕсли;
		
		
	ИначеЕсли ИмяКоИмяКоманды = "ФормаОбновитьДокументИзСбис" Тогда 
		
		
				
	КонецЕсли;
	
	//ТипМетаданных = ПолучитьТипМетаданных(Объекты[0].Объект);
	//Для Каждого ВыгружаемыйДокумент Из Объекты Цикл
	//	Если ЗначениеЗаполнено(ВыгружаемыйДокумент.ИмяИни) Тогда
	//		ИмяИни = "СинхВыгрузка_"+ВыгружаемыйДокумент.ИмяИни;
	//	Иначе	
	//		ИмяИни = "СинхВыгрузка_"+ВыгружаемыйДокумент.ВидОбъекта;	
	//	КонецЕсли;
	//	
	//	API3_ref = Новый Структура;
	//	API3_ref.Вставить("ИдИС",			СокрЛП(ВыгружаемыйДокумент.Объект.УникальныйИдентификатор()) );
	//	API3_ref.Вставить("ТипИС",			ТипМетаданных );
	//	API3_ref.Вставить("ИмяИС",			ВыгружаемыйДокумент.ВидОбъекта );
	//	API3_ref.Вставить("ini_name",		ИмяИни );
	//	API3_ref.Вставить("Название",		СокрЛП(ВыгружаемыйДокумент.Объект) );
	//	API3_ref.Вставить("_print_forms",	?(ВыбранныеПечатныеФормы.Свойство(ВыгружаемыйДокумент.ВидОбъекта),ВыбранныеПечатныеФормы[ВыгружаемыйДокумент.ВидОбъекта],Неопределено));
	//	ОбрабатываемыеОбъекты.Добавить(API3_ref);
	//КонецЦикла;
	//ПараметрыВызова = Новый Структура("МассивОбъектов, Исполнители, ПростойЗапросПодписи", ОбрабатываемыеОбъекты, мИсполнители, ПростойЗапросПодписи);
	//Saby_ФоновыеЗаданияКлиент.ЗапуститьINIФоновымЗаданием("Документы_send", ПараметрыВызова, context_params, ЭтаФорма.ВладелецФормы, "Отправка в СБИС");
	//ЭтаФорма.Закрыть();

	
КонецПроцедуры

&НаКлиенте
Функция МассивСсылокВыделенныхСтрок()
	
	ВыделенныеСтроки = Элементы.Список.ВыделенныеСтроки;
	
	Массив = Новый Массив;
	Для Каждого Строка Из ВыделенныеСтроки Цикл
		Массив.Добавить(Строка.Ссылка);
	КонецЦикла;
	
	Возврат Массив;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСсылкуТест()
	
	СсылкаДок = Документы.ПриемНаРаботу.НайтиПоНомеру("ДС00-000001", ТекущаяДатаСеанса());	
	Возврат СсылкаДок;
	
КонецФункции


&НаСервереБезКонтекста
Функция ПолучитьДанныеПодстановкиИСоздатьДокумент(Ссылка, context_params)
	
	Возврат 
	Документы.Saby_ТранспортнаяНакладная.ПолучитьДанныеПодстановкиИСоздатьДокумент(
		Ссылка, 
		context_params);
	
КонецФункции

#КонецОбласти   

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОтказВПриемкеГруза(Команда)
	
	//Если Объект.Приемка_Отказ Тогда 
	//	Возврат;		
	//КонецЕсли;
	//
	//Подсказка    = "Укажите причину отказа от приемки груза"; 
	//ДопПараметры = Новый Структура;  	
	//Оповещение   = Новый ОписаниеОповещения("ПослеВводаПричиныОтказа", ЭтаФорма, ДопПараметры);
	//
	//ПоказатьВводСтроки(Оповещение, "", Подсказка, 0, Истина);
					
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьВСбис(Команда)
	
	//ОбменДаннымиСБИС(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьВСбис(Команда)
	
	//ОбменДаннымиСБИС(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьСтатусы(Команда)
	
	//ОбменДаннымиСБИС(Команда.Имя);	
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьДокументИзСбис(Команда)
	
	// ToDo: нужен обмен... 
	ОбменДаннымиСБИС(Команда.Имя);
	// Загрузить документ из сбис если были изменения...
	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьСтоимость(Команда)
	
	//ПараметрыФормы = Новый Структура;
	//ПараметрыФормы.Вставить("Отправитель", Объект.Отправитель);
	//
	//ОткрытьФорму(
	//	"Документ.Saby_ТранспортнаяНакладная.Форма.ФормаИзмененияСогласованияСтоимости",
	//	ПараметрыФормы, 
	//	ЭтаФорма,,,,,
	//	РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс); 
	
КонецПроцедуры

&НаКлиенте
Процедура СогласоватьСтоимость(Команда)
	
	// Оповещение
	
	ОткрытьФорму(
		"Документ.Saby_ТранспортнаяНакладная.Форма.ФормаИзмененияСогласованияСтоимости",, 
		ЭтаФорма,,,,,
		РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс); 
		
КонецПроцедуры

&НаКлиенте
Процедура Переадресовка(Команда)
	
	//ОткрытьФорму(
	//	"Документ.Saby_ТранспортнаяНакладная.Форма.ФормаПереадресовка",, 
	//	ЭтаФорма,,,,,
	//	РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);

КонецПроцедуры

&НаКлиенте
Процедура ЗаменаВодителя(Команда)
	
	// Оповещение
	
	//ОткрытьФорму(
	//	"Документ.Saby_ТранспортнаяНакладная.Форма.ФормаЗаменыВодителяИлиТС",, 
	//	ЭтаФорма,,,,,
	//	РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
	
КонецПроцедуры

&НаКлиенте
Процедура УведомлениеОПереадресовке(Команда)
	
		
КонецПроцедуры

#КонецОбласти


// СПИСОК ИЗМЕНЕНИЙ

//last_event = Неопределено;
//	context.params.Свойство("last_event", last_event);
//	Если last_event = Неопределено Тогда
//		last_event = Новый Структура;
//	КонецЕсли;
//	last_event_id = Неопределено;
//	last_event.Свойство("id", last_event_id);
//	
//	last_event_datetime	= Неопределено;
//	last_event.Свойство("date", last_event_datetime);
//	if last_event_datetime = Неопределено and Не ЗначениеЗаполнено(last_event) Тогда
//	    last_event_datetime = local_helper_get_server_datetime(context.params);
//	    context.params.Вставить("last_event", Новый Структура("date", last_event_datetime ));
//	КонецЕсли;
//	
//	//# Формирование фильтра
//	РазмерСтраницы	= 25;
//	Если Не context.params.Свойство("page_size", РазмерСтраницы) Тогда
//		РазмерСтраницы	= 25;
//	КонецЕсли;
//	
//	_filter = Новый Структура(
//	    "Навигация", Новый Структура(
//	        "РазмерСтраницы", Формат(РазмерСтраницы,"ЧГ=0")
//	    )
//	);
//	Если ЗначениеЗаполнено(last_event_datetime) Тогда
//	    _filter.Вставить("ДатаВремяС", last_event_datetime);
//	КонецЕсли;
//	Если ЗначениеЗаполнено(last_event_id) Тогда
//		_filter.Вставить("ИдентификаторСобытия", last_event_id);
//	КонецЕсли;	
//	Попытка
//		events = local_helper_read_changes(context.params, _filter);
//	Исключение
//		ИнфОбОшибке = ИнформацияОбОшибке();
//		Если Найти(ИнфОбОшибке.Описание, "Неверный фильтр. Не найдено событие с идентификатором") Тогда
//			_filter.Удалить("ИдентификаторСобытия");
//			events = local_helper_read_changes(context.params, _filter);
//		Иначе
//			ВызватьИсключение NewExtExceptionСтрока(ИнфОбОшибке,,,,add_block_to_dump(block_context));
//		КонецЕсли;	
//	КонецПопытки;	
//	events = events["Документ"];
//	Если events = Неопределено Тогда
//		events = Новый Массив;
//	КонецЕсли;
//	return events;


