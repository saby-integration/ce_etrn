
&НаКлиенте
Процедура СопроводительныеДокументыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	Если Элемент.ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Элементы.Страница_Груз.ТолькоПросмотр Тогда
		ПараметрыФормы = Новый Структура("Ключ,ТолькоПросмотр", Элемент.ТекущиеДанные.СопроводительныйДокумент, Истина);
		ОткрытьФорму("Справочник.ESD_ДокументыЭПД.Форма.ФормаЭлемента", ПараметрыФормы, ЭтаФорма);	
	КонецЕсли;
КонецПроцедуры

