{"#",4238019d-7e49-4fc9-91db-b6b951d5cf8e,
{3,
{
{"S","ПередВыполнением"},
{"S","{""#"",4238019d-7e49-4fc9-91db-b6b951d5cf8e,
{2,
{
{""S"",""Текст""},
{""S"",""// Обязательные параметры:
//	ТекущийЭтап - Текущий этап документа, для которого закрывается фаза

Если Не ЗначениеЗаполнено(Параметры) Тогда
	ВызватьИсключение """"Не заполнены параметры действия"""";
КонецЕсли;

ПроверяемыйТекущийЭтап = НРег(СокрЛП(Параметры[""""ТекущийЭтап""""]));
Если ПустаяСтрока(ПроверяемыйТекущийЭтап) Тогда
	ВызватьИсключение """"Не заполнен параметр """"""""ТекущийЭтап"""""""""""";
КонецЕсли; 

СтрАктивныйЭтап = СокрЛП(ESD_ТНОбщегоНазначенияСервер.АктивныйЭтапСтрокой(Основание));
Если Не(ЗначениеЗаполнено(СтрАктивныйЭтап) И НРег(СтрАктивныйЭтап) = ПроверяемыйТекущийЭтап) Тогда
	ВызватьИсключение """"<Отказ>"""";
КонецЕсли;

// Сформируем промежуточный результат для следующего обработчика
ПромежуточныйРезультат = Новый Структура;

// Проверим наличие параметров подключения
МодульОбработкиSABY = Обработки.ESD_SABY.Создать();
ПромежуточныйРезультат.Вставить(""""МодульОбработкиSABY"""", МодульОбработкиSABY); 

context_params = МодульОбработкиSABY.ПроверитьНаличиеПараметровПодключения();
Если Не ЗначениеЗаполнено(context_params) Тогда
	ВызватьИсключение """"Не заполнены параметры подключения"""";
КонецЕсли;

// Добавим context_params в промежуточный результат
ПромежуточныйРезультат.Вставить(""""context_params"""", context_params);

// Подготовим данные для выгрузки
ДанныеДляВыгрузки = Новый Массив;
ДанныеДляВыгрузки.Добавить(ESD_ТНОбщегоНазначенияКлиентСервер.ПараметрыВыгрузки(Основание));

// Укажем дополнительные параметры
ДополнительныеПараметры = Новый Структура;
ДополнительныеПараметры.Вставить(""""ДвоичныеДанныеОбработки"""", Неопределено);

// Выполним выгрузку данных документа (СБИС.СгенерироватьВложение и СБИС.ЗаписатьДокумент)
РезультатВыгрузкиSaby = ESD_ТНВыгрузкаСервер.РезультатВыгрузкиДокументов(ДанныеДляВыгрузки, Неопределено, ДополнительныеПараметры);

// Проверим результат выгрузки
Если Не РезультатВыгрузкиSaby.Итог.Успех Тогда
	ВызватьИсключение """"Ошибка при выгрузке данных в Saby. Проверьте журнал регистрации на предмет наличия детализацизированной ошибки отправки"""";
КонецЕсли;
 
// Проверим наличие успешно выгруженных документов
УспешноВыгруженные = РезультатВыгрузкиSaby.Успешные; 
Если Не УспешноВыгруженные.Количество() Тогда
	ВызватьИсключение """"Отсутствуют успешно выгруженные в Saby документы"""";
КонецЕсли;

// Достанем выгруженный документ
ВыгруженныйДокумент = УспешноВыгруженные[0];
 
// Проверим текущий этап
СтрТекущийЭтап = СокрЛП(ВыгруженныйДокумент[""""ТипТитула""""]);
Если НРег(СтрТекущийЭтап) <> ПроверяемыйТекущийЭтап Тогда
	Результат.Отказ = Истина;
	Результат.Комментарий = """"Текущий этап """""""""""" + СтрТекущийЭтап + """""""""""" не соответствует указанному в параметрах"""";
Иначе
	// Добавим список выгруженных документов в промежуточный результат
	ПромежуточныйРезультат.Вставить(""""ДанныеДокумента"""", ВыгруженныйДокумент); 
	
   	Результат.Вставить(""""ПромежуточныйРезультат"""", ПромежуточныйРезультат);
КонецЕсли;""}
},
{
{""S"",""Параметры""},
{""#"",acf6192e-81ca-46ef-93a6-5a6968b78663,
{9,
{2,
{0,""Ключ"",
{""Pattern"",
{""S"",50,1}
},"""",20},
{1,""Тип"",
{""Pattern"",
{""#"",f5c65050-3bbb-11d5-b988-0050bae0a95d}
},"""",20}
},
{2,2,0,0,1,1,
{1,0},1,-1},
{0,0}
}
}
}
}
}"}
},
{
{"S","ПриВыполнении"},
{"S","{""#"",4238019d-7e49-4fc9-91db-b6b951d5cf8e,
{2,
{
{""S"",""Текст""},
{""S"",""// Обязательные параметры:
//	Действие - Строка - название действия, под которым нужно закрыть фазу
//
// Опциональные параметры:
//	Исполнитель - Строка - ФИО исполнителя, под которым выполняется закрытие фазы
//	Комментарий - Строка - комментарий закрытия фазы (при отрицательном закрытии)

Если Не ЗначениеЗаполнено(Параметры) Тогда
	ВызватьИсключение """"Не заполнены параметры действия"""";
КонецЕсли;

ПромежуточныйРезультат = Результат.ПромежуточныйРезультат;

////////////////////////////// Подготовка данных //////////////////////////////

// Получим действие из параметров обработчика
Действие_ = НРег(СокрЛП(Параметры[""""Действие""""]));

// Обработаем отсутствие заполненого действия
Если Не ЗначениеЗаполнено(Действие_) Тогда
	ВызватьИсключение """"Не заполнено действие закрытия фазы"""";
КонецЕсли;

ДанныеДокумента_ = ПромежуточныйРезультат[""""ДанныеДокумента""""];

// Вытащим список параметров этапов из документа
ПараметрыЭтапов_ = ДанныеДокумента_[""""Этап""""];
// Обработаем отсутствие этапов
Если Не ПараметрыЭтапов_.Количество() Тогда
	ВызватьИсключение """"Отсутствуют параметры текущего этапа"""";
КонецЕсли;

// Вытащим параметры этапа из списка
ПараметрыЭтапа_ = ПараметрыЭтапов_[0]; 

// Найдем параметры действия по указанному наименованию
Для Каждого ДоступноеДействие_ Из ПараметрыЭтапа_[""""Действие""""] Цикл
	Если НРег(СокрЛП(ДоступноеДействие_[""""Название""""])) <> Действие_ Тогда
		Продолжить;
	КонецЕсли;
	
	ПараметрыДействия_ = ДоступноеДействие_;
КонецЦикла;

// Обработаем отсутствие параметров действия
Если ПараметрыДействия_ = Неопределено Тогда
	ВызватьИсключение """"Не найдено действие по переданному параметру"""";
КонецЕсли;

Комментарий_ = """""""";
// Проверим необходимость указать комментарий
Если ВРег(ПараметрыДействия_[""""ТребуетКомментария""""]) = """"ДА"""" Тогда
	
	Комментарий_ = СокрЛП(Параметры[""""Комментарий""""]);
	Если Не ЗначениеЗаполнено(Комментарий_) Тогда
		ВызватьИсключение """"Отсутствует комментарий закрытия фазы"""";
	КонецЕсли;
КонецЕсли;
ПараметрыДействия_.Вставить(""""Комментарий"""", Комментарий_);

Исполнители_ = Новый Массив;
// Проверим необходимость указать исполнителя
Если ВРег(ПараметрыДействия_[""""ТребуетИсполнителя""""]) = """"ДА"""" Тогда 
	Для Каждого Исполнитель Из Параметры[""""Исполнители""""] Цикл
		Исполнитель_ = СокрЛП(Исполнитель.Значение);
		Если Не ЗначениеЗаполнено(Исполнитель_) Тогда
			Продолжить;
		КонецЕсли;
		
		Исполнители_.Добавить(Исполнитель_);
	КонецЦикла;	
	
	Если Не ЗначениеЗаполнено(Исполнители_) Тогда
		ВызватьИсключение """"Отсутствует исполнитель закрытия фазы"""";
	КонецЕсли;
КонецЕсли;

ПараметрыДействия_.Вставить(""""Исполнители"""", Исполнители_);

////////////////////////////// Выполнение действия через Blockly //////////////////////////////

context_params = ПромежуточныйРезультат.context_params;
Если context_params = Неопределено Тогда
	context_params = ПромежуточныйРезультат.МодульОбработкиSABY.ПроверитьНаличиеПараметровПодключенияНаСервере();
КонецЕсли;

ПараметрыВызоваБлокли = Новый Соответствие;
ПараметрыВызоваБлокли.Вставить(""""params"""", context_params);
ПараметрыВызоваБлокли.Вставить(""""Algorithm"""", """"Документы_execute"""");

ПараметрыВызоваБлокли.Вставить(""""params"""", context_params);
ПараметрыВызоваБлокли.Вставить(""""CommandsResult"""", Новый Массив);
ПараметрыВызоваБлокли.Вставить(""""CurrentBlock"""", Неопределено);
ПараметрыВызоваБлокли.Вставить(""""Endpoint"""", """""""");
ПараметрыВызоваБлокли.Вставить(""""ContextId"""", Неопределено);
	
ConnectionId = Неопределено;
context_params.Свойство(""""ConnectionUuid"""", ConnectionId);
ПараметрыВызоваБлокли.Вставить(""""ConnectionId"""", ConnectionId);

Документы_ = Новый Массив;
Документы_.Добавить(Новый Структура(
	""""Идентификатор,Документ1С,ДокументСБИС,Этап"""",
	ДанныеДокумента_[""""Идентификатор""""],
	ДанныеДокумента_[""""Ссылка""""],
	,
	ДанныеДокумента_[""""Этап""""]
));
	
EndpointArgs = Новый Структура;
EndpointArgs.Вставить(""""Документы"""", Документы_);
EndpointArgs.Вставить(""""Действие"""", ПараметрыДействия_);
EndpointArgs.Вставить(""""Формы"""", Новый Массив);
EndpointArgs.Вставить(""""Filter"""", Неопределено);
EndpointArgs.Вставить(""""UpdateObjFromIS"""", Ложь);
EndpointArgs.Вставить(""""Этап"""", ПараметрыЭтапа_);
ПараметрыВызоваБлокли.Вставить(""""EndpointArgs"""", EndpointArgs);

ПромежуточныйРезультат.МодульОбработкиSABY.API_BLOCKLY_RUN(ПараметрыВызоваБлокли);""}
},
{
{""S"",""Параметры""},
{""#"",acf6192e-81ca-46ef-93a6-5a6968b78663,
{9,
{2,
{0,""Ключ"",
{""Pattern"",
{""S"",50,1}
},"""",20},
{1,""Тип"",
{""Pattern"",
{""#"",f5c65050-3bbb-11d5-b988-0050bae0a95d}
},"""",20}
},
{2,2,0,0,1,1,
{1,0},1,-1},
{0,0}
}
}
}
}
}"}
},
{
{"S","ПослеВыполнения"},
{"S","{""#"",4238019d-7e49-4fc9-91db-b6b951d5cf8e,
{2,
{
{""S"",""Текст""},
{""S"",""ДанныеДляОбработки = Новый Массив;
ПараметрыЗагрузки = ESD_ТНОбщегоНазначенияКлиентСервер.ПараметрыЗагрузки(Основание);
ПараметрыЗагрузки.ИзДокумента = Ложь;
ПараметрыЗагрузки.ИмяМетаданных = Основание.Метаданные().Имя;
ПараметрыЗагрузки.ЗагрузкаНаКлиенте = Ложь;
ПараметрыЗагрузки.ЭтоЗагрузкаЧерновика = Ложь;

ДанныеДляОбработки.Добавить(ПараметрыЗагрузки);

ДополнительныеПараметры = Новый Структура;
ДополнительныеПараметры.Вставить(""""ДвоичныеДанныеОбработки"""", Неопределено);
	
ESD_ТНЗагрузкаСервер.РезультатЗагрузкиДокументов(ДанныеДляОбработки, Неопределено, ДополнительныеПараметры);""}
},
{
{""S"",""Параметры""},
{""#"",acf6192e-81ca-46ef-93a6-5a6968b78663,
{9,
{2,
{0,""Ключ"",
{""Pattern"",
{""S"",50,1}
},"""",20},
{1,""Тип"",
{""Pattern"",
{""#"",f5c65050-3bbb-11d5-b988-0050bae0a95d}
},"""",20}
},
{2,2,0,0,1,1,
{1,0},1,-1},
{0,0}
}
}
}
}
}"}
}
}
}